index.html.erb
<h2>My tasks</h2>
<%= link_to 'New', new_task_path  %>

<ul>
  <% @tasks.each do |task| %>
  <li>
    <%= task.title %>
    <%= link_to task.title, task_path(task) %>
  </li>
<% end %>
</ul>

